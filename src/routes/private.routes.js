const express = require('express');
const router = express.Router();


// const bankAccounts = require("../controllers/bankAccount.controller")

// router.delete("/budget-recommendations/:id", budgetRecommendationController.deleteBudgetRecommendation);

module.exports = router;